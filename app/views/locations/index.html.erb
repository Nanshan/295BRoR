<h1>Listing location logs</h1>

<table>
  <thead>
    <tr>
      <th>UserID</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th>CreatedTime</th>
    </tr>
  </thead>

  <tbody>
    <% @locations.each do |location| %>
      <tr>
        <td><%= location.userID %></td>
        <td><%= location.latitude %></td>
        <td><%= location.longitude %></td>
        <td><%= location.created_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>


